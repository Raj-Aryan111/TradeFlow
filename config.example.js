// config.example.js — copy this to config.js and add real key
// config.js is gitignored, so key never gets committed.
const CONFIG = {
  API_KEY: 'YOUR_FINNHUB_API_KEY',
  POLL_INTERVAL_MS: 12000,
};
